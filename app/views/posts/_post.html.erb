<div id="<%= dom_id post %>">
  <h2>
    <%= post.title %>
  </h2>
  <h3>Posted by <%= post.user.email %></h3>
  <span><%= pluralize(post.views, 'view') %></span>
  <p>
    <%= post.body %>
  </p>
</div>
